{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware for campus notification app",
  "main": "index.ts",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
