# Notification App Backend

This directory is reserved for the backend application.

For the Frontend Track submission, the backend logic is handled via direct API calls to the evaluation server from the frontend application.

The `logging_middleware` package is consumed by the frontend (`notification_app_fe`) for all logging operations.
